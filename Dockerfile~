FROM node:8-alpine


WORKDIR /root/

ADD . /root/sentinel

ADD run.sh /root/

# RUN apt-get clean && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*

RUN apk update && apk upgrade && apk add g++ gcc gmp-dev libffi-dev make python2 python3 mongodb redis nano git  && mkdir -p /data/db

# RUN node -v && npm -v && mongo --version

# RUN  apk update && apk add python-minimal mongodb redis-server -yy gcc make node-gyp curl nano

# RUN cd

# RUN curl -sL https://deb.nodesource.com/setup_8.x -o nodesource_setup.sh &&  bash nodesource_setup.sh && apt-get update &&\
# apt-get install -y nodejs && nodejs -v && npm -v

CMD [ "sh", "run.sh" ]

EXPOSE 3000
